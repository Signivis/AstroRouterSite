---
import Layout from "../../layouts/Layout.astro";
import RouterCard from "../../components/RouterCard.astro";
import { getAllRouters, getUITranslations } from "../../lib/db";
import { isValidLocale, t, DEFAULT_UI, type Locale } from "../../lib/i18n";

const { lang } = Astro.params;

if (!lang || !isValidLocale(lang) || lang === "en") {
  return Astro.redirect("/");
}

const locale = lang as Locale;
const [routers, dbUI] = await Promise.all([
  getAllRouters(locale),
  getUITranslations(locale),
]);
const ui = { ...DEFAULT_UI, ...dbUI };
---

<Layout title={t(ui, "nav.home")} description={t(ui, "hero.description")} locale={locale} ui={ui}>
  <!-- Hero Section -->
  <section class="bg-gradient-to-br from-blue-600 via-blue-700 to-indigo-800 text-white">
    <div class="max-w-screen-xl mx-auto px-4 py-16 md:py-24 text-center">
      <h1 class="mb-4 text-4xl font-extrabold tracking-tight leading-none md:text-5xl lg:text-6xl">
        {t(ui, "hero.title")}
      </h1>
      <p class="mb-8 text-lg font-normal text-blue-100 lg:text-xl sm:px-16 lg:px-48">
        {t(ui, "hero.description")}
      </p>
      <div class="flex flex-col space-y-4 sm:flex-row sm:justify-center sm:space-y-0 sm:space-x-4">
        <a href="#routers" class="inline-flex justify-center items-center py-3 px-5 text-base font-medium text-center text-white rounded-lg bg-blue-500 hover:bg-blue-400 focus:ring-4 focus:ring-blue-300 transition-colors">
          {t(ui, "hero.cta")}
          <svg class="w-3.5 h-3.5 ms-2 rtl:rotate-180" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 10">
            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="bg-white border-b border-gray-200">
    <div class="max-w-screen-xl mx-auto px-4 py-8">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 text-center">
        <div>
          <dt class="text-3xl md:text-4xl font-extrabold text-blue-600">10</dt>
          <dd class="text-gray-500 font-medium mt-1">{t(ui, "stats.top_routers")}</dd>
        </div>
        <div>
          <dt class="text-3xl md:text-4xl font-extrabold text-blue-600">7</dt>
          <dd class="text-gray-500 font-medium mt-1">{t(ui, "stats.major_brands")}</dd>
        </div>
        <div>
          <dt class="text-3xl md:text-4xl font-extrabold text-blue-600">WiFi 7</dt>
          <dd class="text-gray-500 font-medium mt-1">{t(ui, "stats.latest_standard")}</dd>
        </div>
        <div>
          <dt class="text-3xl md:text-4xl font-extrabold text-blue-600">4.5</dt>
          <dd class="text-gray-500 font-medium mt-1">{t(ui, "stats.avg_rating")}</dd>
        </div>
      </div>
    </div>
  </section>

  <!-- Router Grid -->
  <section id="routers" class="max-w-screen-xl mx-auto px-4 py-12">
    <div class="text-center mb-10">
      <h2 class="text-3xl font-bold text-gray-900 mb-3">{t(ui, "collection.title")}</h2>
      <p class="text-gray-500 text-lg">{t(ui, "collection.description")}</p>
    </div>

    {routers.length === 0 ? (
      <div class="text-center py-16">
        <svg class="mx-auto w-16 h-16 text-gray-300 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"/>
        </svg>
        <h3 class="text-xl font-semibold text-gray-500">{t(ui, "empty.title")}</h3>
        <p class="text-gray-400 mt-2">{t(ui, "empty.description")}</p>
      </div>
    ) : (
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {routers.map((router) => (
          <RouterCard router={router} locale={locale} ui={ui} />
        ))}
      </div>
    )}
  </section>
</Layout>
